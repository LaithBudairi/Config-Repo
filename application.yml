app:
  name: My app
  description: Welcome to ${app.name}

my:
  greeting: updated greeting from config server 3
  list:
    value: One,Two,Three,Four

db:
  connection: "{connectionString: 'http://___', userName: 'foo', password: 'pass123'}"
  host: 127.0.0.1
  port: 1200

management:
  endpoints:
    web:
      exposure:
        include: "*"

